# Configuración mínima para el contenedor Mosquitto del proyecto.
# Permite conexiones desde cualquier dispositivo de la red local
# (necesario para que el ESP32/simulador se conecte al broker).

listener 1883 0.0.0.0
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest file /mosquitto/log/mosquitto.log